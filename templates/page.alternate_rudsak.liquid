<div class="page-width">
    <div class="grid">
        <div class="grid__item medium-up--one-half">
            <div class="section-header text-center">
                {{ page.title }}
            </div>

            <div class="rte">
                {% section 'alternate_rudsak' %}
            </div>
        </div>
    </div>
</div> 